# 凭证 — 绝不入库
.env
*.pem
*.key

# Base 探查产物可能含真实客户数据
schema_snapshot*.json
!schema_snapshot.example.json
out/

# 业务数据导出，含真实客户信息，绝不入库
*.xlsx
*.xls
*.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# 主目录里手工放进来的临时产物，不是项目内容
/CRM-BaseBot-*.zip
/UID

# 每日跑批的产物：抓下来的附件（*.xlsx 已在上面忽略）和 launchd 日志
/attachments/
/logs/
